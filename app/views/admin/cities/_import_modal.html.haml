.modal.fade#importModal{ tabindex: -1, role: 'dialog', aria: { labelledby: 'city_importModalLabel'} }
  .modal-dialog{ role: 'document' }
    .modal-content
      .modal-header
        %button.close{ aria: { label: 'Close' }, data: { dismiss: 'modal' }, type: 'button' }
          %span{ aria: { hidden: true } } ×
        %h4.modal-title#myModalLabel
          = t('shared.import_title')
        %p
          = t('admin.cities.import_instruction')
        = link_to t('shared.instruction'), admin_upload_instructions_path
      = form_with url: import_admin_cities_path, multipart: true, local: true, class: 'form-horizontal' do |f|
        .modal-body
          .form-group
            .col-sm-3.control-label
              = f.label :region_id, "#{t('activerecord.models.region.one')}:", class: 'control-label'
            .col-sm-9
              = f.select :region_id, available_regions, class: 'form-control'
          .form-group
            .col-sm-3.control-label
              = f.label :file, t('shared.import_title'), class: 'control-label'
            .col-sm-9
              = f.file_field :file
        .modal-footer
          = f.submit t('admin.file.import_button'), class: 'btn btn-success'
          %button.btn.btn-default{ data: { dismiss: 'modal' }, type: 'button' }
            = t('cancel')
