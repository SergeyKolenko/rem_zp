.js-modal-window.modal.fade{ aria:{ labelledby: 'type_createModalLabel' }, role: 'dialog', tabindex: '-1' }
  .modal-dialog{ role: 'document' }
    .modal-content
      .modal-header
        %button.close{ data: { dismiss: 'modal'}, aria: {label: 'Close' } }
          %span{ aria: { hidden: true} } ×
        %h4.modal-title#myModalLabel
          = t('edit')
      = form_with scope: Type.new, url: admin_types_path, method: :post, class: 'form-horizontal js-modal-form', local: true do |f|
        .modal-body
          .form-group
            = f.label :name, class: 'col-sm-3 control-label'
            .col-sm-9
              = f.text_field :name, class: 'form-control', required: true
          .form-group
            = f.label :code, class: 'col-sm-3 control-label'
            .col-sm-9
              = f.text_field :code, class: 'form-control', required: true
        .modal-footer
          = f.submit t('save'), class: 'btn btn-success'
          %button.btn.btn-default{ data: { dismiss: 'modal' }, type: 'button' }
            = t('cancel')