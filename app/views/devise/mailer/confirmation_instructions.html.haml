%p
  =t('devise.mailer.confirmation_instructions.welcome', name: @resource.full_name)
%p
  =t('devise.mailer.confirmation_instructions.role_descr', role: @resource.role_name)
%p
  =t('devise.mailer.confirmation_instructions.accept_acc')
%p
  = link_to t('devise.mailer.confirmation_instructions.accept'), confirmation_url(@resource, confirmation_token: @token)
