%p
  =t('devise.mailer.confirmation_instructions.welcome', name: @resource.full_name)
%p
  =t('devise.mailer.password_change.some_change')
%p
  = link_to t('devise.mailer.password_change.change'), edit_password_url(@resource, reset_password_token: @token)
%p
  =t('devise.mailer.password_change.ignore')
%p
  =t('devise.mailer.password_change.new_pass')
