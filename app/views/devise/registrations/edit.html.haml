.row
  %h2.text-center Редактировать информацию
  .col-xs-8.col-sm-8.col-md-10
    = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
      .text-center= devise_error_messages!
      = f.hidden_field :reset_password_token
      .form-group
        .col-sm-8.col-sm-offset-3
          = f.label :email, 'Адрес электронной почты' ,class: 'control-label'
          = f.email_field :email, autofocus: true, class: 'form-control', required: true
          - if devise_mapping.confirmable? && resource.pending_reconfirmation?
            %div "Ваш электронный адрес #{resource.unconfirmed_email} не подвержден."
      .form-group
        .col-sm-8.col-sm-offset-3
          = f.label :password, 'Пароль', class: 'control-label'
          %i Оставьте поле пустым, если не желаете менять пароль.
          = f.password_field :password, autocomplete: 'off', class: 'form-control'
      .form-group
        .col-sm-8.col-sm-offset-3
          = f.label :password, 'Введите пароль повторно', class: 'control-label'
          = f.password_field :password_confirmation, autocomplete: 'off', class: 'form-control', required: true
      .form-group
        .col-sm-8.col-sm-offset-3
          = f.label :current_password, 'Пароль', class: 'control-label'
          %i Чтобы подтвердить изменения нам необходим ваш текущий пароль.
          = f.password_field :current_password, autocomplete: 'off', class: 'form-control'
      .form-group
        .col-sm-8.col-sm-offset-3.mart-5
          = f.submit 'Обновить информацию', class: 'btn btn-success'
          .mart-5
            = render 'devise/shared/links'
      %h3.col-sm-offset-3 Удалить аккаунт:
      .col-sm-offset-3
        = button_to 'Удалить аккаунт', registration_path(resource_name), data: { confirm: 'Вы уверены?' }, method: :delete
      .col-sm-offset-3
        = link_to 'Вернуться назад', :back
